{
  "name": "host",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Consumer service: imports functions from a machine-backed Module Federation remote.",
  "scripts": {
    "build": "rsbuild build",
    "test": "pnpm build && node --test test/*.test.mjs",
    "start": "node dist/index.js",
    "serve": "node dist/server.js",
    "bindgen": "node node_modules/@federated-compute/machinen-plugin/dist/cli.js"
  },
  "dependencies": {
    "@federated-compute/machinen-plugin": "workspace:*",
    "@module-federation/runtime": "^2.5.1"
  },
  "devDependencies": {
    "@machinen/runtime": "0.6.1",
    "@rsbuild/core": "^2.0.9",
    "@types/node": "^22",
    "typescript": "^5.7.0"
  }
}
